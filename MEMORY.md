# Memory: 月影防衛線 — KAGEKIRI PROTOCOL

## 2026-08-17: 方向性の合意

ユーザーは既存のSpace Invaders風Webゲームを、同人ゲーム水準まで高めることを希望した。選択された表現は**ネオン和風サイバーSF**であり、作品仮題を『月影防衛線 — KAGEKIRI PROTOCOL』と定めた。想定プレイ環境は、Windows、macOS、Linux、Android、iOSのブラウザである。

既存のMVPはHTML、CSS、単一の`main.js`で構成され、プレイヤーの左右移動・射撃、隊列移動する敵、敵弾、バリケード、ライフ、スコア、タイトル／結果画面を持つ。次の版では、この単一ファイル構成をES Modulesへ分割し、`deltaTime`を用いるゲームループ、明示的な状態遷移、セマンティックな入力処理、モバイル用の仮想操作UIへ置き換える。

## アート上の決定

視覚基準として`/home/ubuntu/neon_wafuu_visual_target.png`を生成した。深い藍色の夜を背景に、朱・紫・金の妖怪型ドローンと、青緑に光る結界機・御札ショットを対比させる。鳥居、五重塔、障子、御札、鬼面、狐面、蛇霊、提灯をサイバー意匠に変換する。ゲームとしての可読性を優先するため、背景は暗く、当たり判定を持つオブジェクトは明瞭な輪郭・色差・余白を保つ。

## 実装上の注意

- 効果音は、ブラウザの自動再生制限を避けるため、開始操作または最初の入力の後だけ有効にする。
- モバイルではタッチ領域をCanvasの三分割にする方式ではなく、指が画面を隠しにくい常設の仮想操作UIへ変更する。
- 画面サイズはCSSでフィットさせつつ、内部解像度は16:9を維持する。
- 大量の画像アセットに依存せず、Canvas描画を主体にして読み込み時間とモバイル性能を確保する。
- 公開先は既存のGitHub Pagesを維持する。将来PWA化する場合も、通常のブラウザ版が先に完成していることを前提とする。
